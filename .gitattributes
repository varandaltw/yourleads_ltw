# Normalize text files to LF
* text=auto

# Handle Python and shell scripts with LF line endings
*.py text eol=lf
*.sh text eol=lf

# Treat image files as binary
*.jpg binary
*.png binary
*.gif binary

# Use Git LFS for large assets
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.psd filter=lfs diff=lfs merge=lfs -text

media/* filter=lfs diff=lfs merge=lfs -text

# Prevent Git from showing diffs for minified files
*.min.js -diff

# Use custom diff settings for code files
*.cpp diff=cpp
*.js diff=javascript
